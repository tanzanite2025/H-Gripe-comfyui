# Tauri build artifacts
src-tauri/gen/
src-tauri/target/

# Node (frontend tooling, if added later)
node_modules/

# Generated front-end build (studio-ui -> served as frontendDist ../dist).
# H-Gripe Desktop is a single Vite + React app under studio-ui/; its build
# output is produced by the Tauri before* hooks, not committed.
dist/
